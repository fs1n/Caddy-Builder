{
    # Disable automatic HTTPS so Caddy won't try to obtain certs for unknown hostnames
    auto_https off
}

:80 {
    root * /srv
    file_server browse
    # Fallback 404 for directories without index.html
}